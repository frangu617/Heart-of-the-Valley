// src/data/events/chapter1/types.ts
// Re-export canonical event types to avoid duplicate GameplayFlag unions.
export type {
  GameplayFlag,
  EventConditions,
  CharacterEvent,
  EventHistory,
  CharacterEventState,
} from "../types";
